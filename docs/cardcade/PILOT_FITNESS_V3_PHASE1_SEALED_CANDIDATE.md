# V3 Phase 1 sealed candidate packet

**SEALED CANDIDATE FOR HQ REVIEW / SCORING NOT AUTHORIZED.**

This packet preserves the eight candidates from `e5bc61c` and seals them only
after offline transformation, replay, oracle-membership, seat-counterpart and
privacy checks. Specification V3 remains authoritative at `c18a8fc`; the engine
and both Pilots remain frozen.

## Canonical set

`F1 = 8`:

- Main Action: one T and one B;
- Attack: one T and one B;
- Blocks: one T and one B;
- Priority: one T and one B.

Each T branch was executed without Pilot policy through its named horizon and
has an independent acceptable set. Each B branch preserves a genuine pass-only
boundary. U is `NOT APPLICABLE` for these direct surfaces under the structural
record sealed in the Phase 1 inventory.

## R applicability

No R fixture is invented for Phase 1. The eight canonical candidates claim only
T and B competencies. The explicit disposition is
`NO_R_CANDIDATE_IN_PHASE1_SCOPE`; future R coverage requires a separate V3
witness and does not alter F1. This is a scope disposition, not a pooled pass or
a claim that the hooks can never expose resource judgment.

## Seal checks

For every candidate, the packet records:

- recipient-specific view and complete legal option set;
- every relevant option branch, horizon and oracle membership;
- reversed legal-option ordering with invariant branch membership;
- canonical runtime-object-ID normalization and renamed-ID digest;
- duplicate reconstruction digests;
- a structural player-index counterpart with the same card/option relations and
  oracle membership;
- privacy eligibility, required pair ID and the preservation contract.

All 16 canonical seat versions are privacy-eligible under the declared
perturbation: opponent-private library identities may change while public view,
hand sizes, legal options and the deciding player's own-deck binding remain
fixed. Thus `K1 = 16`.

The informational Phase 1 invocation subtotal is:

```text
24F1 + 4K1 = 24*8 + 4*16 = 256
```

This subtotal is not a scoring authorization. No Pilot output exists, and no
call schedule may be used until HQ separately authorizes scoring after reviewing
the packet.

The companion JSON and reconstruction script carry the exact branch payloads,
digests, transformations and privacy records. All artifact sidecars use
canonical UTF-8/LF bytes.
