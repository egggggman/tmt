# Preserve Calibration V1 first-execution stop

Preserves `CALIBRATION_V1_20260914T224952Z_c3e745631702` exactly as produced at release `bf3ace0ebe984dbd24cf825dcc2bd32fb2da39af`, using frozen runtime `425fd1dff2925454d10c113a32a662e58fda1544`.

The first execution of `b0000-p00-canonical` stopped with `executor returned without terminal state`. One execution was attempted, zero members authenticated, and no duplicate or later execution ran. No balance analysis was performed.

The original stop, execution ledger, traceback, available raw result, frozen-input manifest and SHA-256 sidecars are unchanged. The original launcher is archived byte-for-byte as `.py.txt`; its hash is bound by the run manifest. Git attributes disable line-ending conversion for this evidence directory.

This PR contains preservation evidence only. It does not authorize another calibration run or alter runtime, seeds, or decks.
